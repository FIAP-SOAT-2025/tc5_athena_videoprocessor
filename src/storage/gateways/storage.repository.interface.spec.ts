describe('StorageRepositoryInterface', () => {
  it('should be a valid interface definition', () => {
    // Interfaces only exist at compile time, this test verifies the file can be imported
    expect(true).toBe(true);
  });
});
